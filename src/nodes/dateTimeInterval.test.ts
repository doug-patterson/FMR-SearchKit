describe('date time interval filter', () => {
  // tests for this have to be written to fake some data based on 
  // subtracting periods from the current date and then making fake
  // results to test the results against also calculated from Date.now() - too
  // much work for too little payoff
  it('is not tested', () => {
    expect(1).toBe(1)
  })
})

