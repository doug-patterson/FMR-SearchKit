describe('date time interval filter', () => {
  // to do - the named intervals will require constructing test data and result data
  // from Date.now() on the fly
  it('is not tested', () => {
    expect(1).toBe(1)
  })
})

